# Copy this file to .env.local and adjust if your backend runs elsewhere.
# .env.local is git-ignored by Next.js's default .gitignore.

# Must be prefixed with NEXT_PUBLIC_ to be readable in the browser,
# since this is a client-side fetch call.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
